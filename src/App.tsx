import React, { FC } from "react";
import Form from "./components/reminder-form/reminder-form";

const App: FC = () => {
  return (
    <div className="App">
      <Form />
    </div>
  );
};

export default App;
